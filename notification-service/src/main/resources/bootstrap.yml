spring:
  application:
    name: notification-service # Tên của microservice
  cloud:
    config:
      uri: http://localhost:8888  # Địa chỉ của Config Server
      profile: default
      label: main
